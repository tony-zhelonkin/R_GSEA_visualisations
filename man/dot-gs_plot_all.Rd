% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-all.R
\name{.gs_plot_all}
\alias{.gs_plot_all}
\title{Render the standard plot set for a gene-set result}
\usage{
.gs_plot_all(
  x,
  out_dir,
  name = "gsea",
  top_n = 20,
  padj_cutoff = 0.05,
  width = NULL,
  height = NULL,
  verbose = FALSE
)
}
\arguments{
\item{x}{A \link{gs_result}.}

\item{out_dir}{Output directory; created if missing.}

\item{name}{Stem prepended to every file name.}

\item{top_n}{Number of pathways per plot.}

\item{padj_cutoff}{FDR threshold for highlighting and for the log.}

\item{width}{Figure width in inches, or \code{NULL} to take a per-database
suggestion from \code{\link[=gs_plot_size]{gs_plot_size()}}.}

\item{height}{Figure height in inches, or \code{NULL} for the same.}

\item{verbose}{Logical. Report progress with \code{\link[=message]{message()}}.}
}
\value{
A character vector of every written path, invisibly.
}
\description{
The body behind the deprecated \code{plot_all_gsea_results()}, rebuilt on the new
renderers: for each database in \code{x} it writes an up dotplot, a down dotplot,
a direction-faceted dotplot, a barplot and a text log under
\verb{<out_dir>/<database>/}. Every figure goes through \code{\link[=gs_save]{gs_save()}}, so each one
arrives with its source table beside it.
}
\details{
Internal on purpose -- the new golden path is to call the renderer you want
and save it. This exists so the deprecation shim has something to delegate
to.
}
\keyword{internal}
