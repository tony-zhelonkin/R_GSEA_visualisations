% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-size.R
\name{gs_scale_fonts}
\alias{gs_scale_fonts}
\title{Scale a figure's text to its canvas}
\usage{
gs_scale_fonts(plot, width, height, base_font_size = 10)
}
\arguments{
\item{plot}{A \code{ggplot} or \code{patchwork}.}

\item{width, height}{Canvas size in inches -- the values you will pass to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{base_font_size}{Numeric. Reference point size at the 7 x 5 in
reference canvas.}
}
\value{
The plot with a text-size theme applied. For a \code{patchwork} the
theme is applied to every panel with patchwork's \code{&}.
}
\description{
Point sizes are absolute; canvas sizes are not. A theme built at 14 pt reads
well on a 7 x 5 in figure and looks oversized on a 9 x 10 in one, which is
how a renderer that never sees the canvas ends up producing labels that
crowd out the panel. This applies one scale factor,
\code{sqrt((width * height) / (7 * 5))}, to every text element, so the \emph{relative}
typography a renderer chose survives and only the absolute size moves.
}
\details{
It works on a plain \code{ggplot} and on a \code{patchwork}, and it does not care who
built the object. Use it on figures this package did not draw.

The scaled sizes are absolute, so they supersede any base-size floor,
including \code{\link[=theme_bulki]{theme_bulki()}}'s 14 pt.
}
\examples{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) + ggplot2::geom_point()
gs_scale_fonts(p, width = 12, height = 9)
}
\seealso{
\code{\link[=gs_save]{gs_save()}}, \code{\link[=gs_plot_size]{gs_plot_size()}}
}
