% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-utils.R
\name{.gs_placeholder_name}
\alias{.gs_placeholder_name}
\title{Is a display name a placeholder rather than a name?}
\usage{
.gs_placeholder_name(x)
}
\arguments{
\item{x}{Character vector of candidate display names.}
}
\value{
A logical vector, \code{TRUE} where \code{x} carries no usable name.
}
\description{
A \code{pathway_name} is meant to be the reader-facing label, so a renderer
treats it as authoritative. That breaks when a provider wrote a placeholder:
\code{coresh_derived_sets.gmt} ships \code{-} in every description field, and the
resulting legend read \code{-} five times. Anything with no letter or digit in it
cannot be a name, so it falls back to the machine id.
}
\details{
Applied to already-cached results too, which is the point: a cache built by
an older provider must still render, without being rebuilt.
}
\keyword{internal}
