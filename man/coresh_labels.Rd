% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coresh-labels.R
\name{coresh_labels}
\alias{coresh_labels}
\title{Web-UI-style labels for CoReSh-derived gene sets}
\usage{
coresh_labels(
  x,
  titles = NULL,
  style = c("ui", "compact", "title"),
  title_width = 60L
)
}
\arguments{
\item{x}{A data frame carrying at least \code{gse}. Recognised optional columns:
\code{gpl}, \code{pct_var}, \code{p_value}, \code{query_size} or \code{size}, \code{rank} or
\code{rank_in_coresh}, and \code{set_name}. Both a \code{\link[=coresh_search]{coresh_search()}} result and a
\code{\link[=coresh_sets]{coresh_sets()}} \code{provenance} attribute satisfy this.}

\item{titles}{Optional GEO titles: a named character vector (\code{gse} ->
title), or a data frame with \code{gse} and \code{title} columns. Titles are not
fetched -- there is no network call here -- so pass the lookup your
project already keeps.}

\item{style}{One of:
\itemize{
\item \code{"ui"} (default) -- title, then accession, platform, size and percent of
variation, mirroring the web interface's columns.
\item \code{"compact"} -- accession, platform and percent of variation only.
\item \code{"title"} -- the GEO title alone, falling back to the accession.
}}

\item{title_width}{Integer. Titles longer than this are truncated with an
ellipsis, because a GEO title can run to 200 characters. \code{Inf} keeps them
whole; renderers wrap rather than truncate, so \code{Inf} is safe there.}
}
\value{
A character vector of labels, named by \code{set_name} when \code{x} has that
column and by \code{gse} otherwise. Feed it to \code{gsdb_register(pathway_names =)}
or to a renderer's \verb{labels =}.
}
\description{
A CoReSh set id encodes its provenance -- \verb{CORESH_<query_name>_<GSE>} --
which makes it a poor axis label twice over. It repeats the database name on
a figure that is already a CoReSh figure, and it shows the \emph{query} name,
which for a DE-seeded query is the caller's own contrast name, against an
unrelated external GEO dataset. A reader sees a familiar contrast label and
reasonably concludes the bar is about that contrast.
}
\details{
This composes the label the CoReSh web interface shows instead, from the
columns \code{\link[=coresh_search]{coresh_search()}} and \code{\link[=coresh_sets]{coresh_sets()}} already return: GEO title (if
you supply one), accession, platform, query size and percent of variation.
}
\examples{
hits <- tibble::tibble(
  set_name = c("CORESH_Q_iron_GSE1", "CORESH_Q_iron_GSE2"),
  gse = c("GSE1", "GSE2"),
  gpl = c("GPL570", "GPL1261"),
  pct_var = c(0.4012, 0.3550),
  query_size = c(20L, 20L)
)
coresh_labels(hits)
coresh_labels(hits, titles = c(GSE1 = "Iron overload in macrophages"))
}
\seealso{
\code{\link[=coresh_search]{coresh_search()}}, \code{\link[=coresh_sets]{coresh_sets()}}, \code{\link[=gsdb_register]{gsdb_register()}}
}
