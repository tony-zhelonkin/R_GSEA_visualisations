% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs-plot-running.R
\name{.grs_frames}
\alias{.grs_frames}
\title{Reshape \code{\link[fgsea:plotEnrichmentData]{fgsea::plotEnrichmentData()}} output into per-panel frames}
\usage{
.grs_frames(curves, ids)
}
\arguments{
\item{curves}{Named list of \code{plotEnrichmentData()} outputs.}

\item{ids}{Character vector of pathway ids, in plotting order.}
}
\value{
A list of three data frames: \code{es}, \code{ticks}, \code{stats}.
}
\description{
Tick lanes span their whole lane: lane \code{i} covers \verb{[n - i, n - i + 1]}, so
with the tick panel's limits pinned to \code{c(0, n)} every tick is \code{1/n} of that
panel's height regardless of how many pathways are drawn. The previous
version used \verb{[n - i + 0.2, n - i + 0.8]} inside a rescaled window, which
left each tick at roughly 2\% of the panel and made them unreadable.
}
\keyword{internal}
