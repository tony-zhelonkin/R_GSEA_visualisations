% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene-ids.R
\name{.gene_ids_map}
\alias{.gene_ids_map}
\title{Map keys through an organism annotation package}
\usage{
.gene_ids_map(db, keys, keytype, column, multi_vals)
}
\arguments{
\item{db}{An \code{OrgDb} object.}

\item{keys}{Character vector of unique keys.}

\item{keytype, column}{Character(1) keytype to look up and column to return.}

\item{multi_vals}{Passed to \code{\link[AnnotationDbi:mapIds]{AnnotationDbi::mapIds()}} as \code{multiVals}.}
}
\value{
A named character vector of hits, possibly empty. Names are the
matched keys.
}
\description{
Restricts \code{keys} to those the requested keytype actually knows before
calling \code{\link[AnnotationDbi:mapIds]{AnnotationDbi::mapIds()}}. Filtering first avoids two behaviours
that make an unfiltered call unusable as a fallback: \code{mapIds()} errors
outright when \emph{no} key is valid, and emits an unsuppressible warning when
only some are.
}
\keyword{internal}
